Small Linux CLI tools written in bash and C.
Focus: process inspection, debugging, learning.

watch-proc: Used to track processes and their children based on parent's name
e.g. "watch-proc bash" would track any processes containing bash in their command name
